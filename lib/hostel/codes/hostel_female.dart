import 'package:flutter/material.dart';

class HostelFemale extends StatelessWidget {
  const HostelFemale({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}